[tool:pytest]
markers =
    real_api: marks tests as using real API calls (may be slow, requires API keys)
    mock_api: marks tests as using mocked API calls (fast, no API keys needed)

# By default, skip real API tests unless explicitly requested
addopts = -m "not real_api"

# Show extra test summary info
addopts = -v --tb=short

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*